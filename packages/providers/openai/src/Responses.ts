// The OpenAI Responses provider lives in `@effect-uai/responses` (the protocol
// is an open standard, not OpenAI-specific). Re-exported here so the OpenAI
// brand package is a single install for the whole OpenAI stack.
export * from "@effect-uai/responses/Responses"
