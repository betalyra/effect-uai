packages:
  - "packages/*"
  - "packages/providers/*"
  - "recipes"
  - "webpage"
  - "integration-tests/*"
  # Excluded: standalone packages with heavy native deps. Install with
  # `pnpm install --ignore-workspace` from inside each folder.
  - "!integration-tests/sandbox-microsandbox"
