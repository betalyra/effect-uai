import { describe, expect, it } from "vitest"

describe("auto-compaction", () => {
  it.todo("compacts when turn count crosses threshold")
  it.todo("compacts when cumulative tokens cross threshold")
  it.todo("preserves cache breakpoint placement after compaction")

  it("placeholder — recipe scaffold compiles", () => {
    expect(true).toBe(true)
  })
})
