import { describe, expect, it } from "vitest"

describe("basic-usage", () => {
  it.todo("runs a turn, executes the requested tool, and stops on the final answer")

  it("placeholder - recipe scaffold compiles", () => {
    expect(true).toBe(true)
  })
})
export {}
