# Skills

This folder will hold AI agent skills (e.g. for Claude Code) that teach an
assistant how to apply `effect-uai` primitives and recipes when helping
users build agents in their own codebase.

Currently empty - see `plans/` and `recipes/` for the patterns these skills
will eventually wrap.
